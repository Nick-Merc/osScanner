class TargetData:
	def __init__(self, name, addresses, nameservers, ports, webserver, whoisData):
		self.name = name
		self.addresses = addresses
		self.nameservers = nameservers
		self.ports = ports
		self.webserver = webserver
		self.whoisData = whoisData